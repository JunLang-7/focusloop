/**
 * Imported material, normalised.
 *
 * One deterministic shape for text, Markdown and extracted PDF text, so the same input always
 * produces the same material and therefore the same course.
 */

export * from './parse-material';
